%h1 Listing conversations

%table.table
	%tr
		%th Name
		%th
		%th
		%th

	- @conversations.each do |user_conversation|
		%tr
			%td= link_to user_conversation.subject, conversation_path(user_conversation.id)
			%td= link_to 'Remove', conversation_path(user_conversation.id), :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Conversation', new_conversation_path
