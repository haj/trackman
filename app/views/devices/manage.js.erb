// Append new data
$("tbody#manage-device-tbody").append("<%= j render(:partial => '/devices/partials/manage_devices_rows', locals: { devices: @devices }) %>")

// Update pagination link
<% if @devices.last_page? %>
  $('.pagination').html("No More Data to Show!");
<% else %>
  $('.pagination')
    .html("<%=j link_to_next_page(@devices, 'Load more data', :remote => true) %>");
<% end %>
