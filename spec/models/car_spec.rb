require 'spec_helper'

describe Car do

	describe "cars_without_devices" do 
		it "should return a bunch of cars that doesn't have devices attached to them" 
		it "should return the car in addition to other availables cars if the device already has a car"
		it "should return an empty array if there is no cars without devices"
	end

end